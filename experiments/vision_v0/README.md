# Vision V0 Experiment

Deferred until customer search value is demonstrated.

Initial target:
One display-case image → candidate object crops.

Primary metric:
human seconds required to produce 100 useful publishable item representations.

Do not solve persistent multi-day inventory reconciliation in this experiment.
