# Search V0 Experiment

Purpose:
Evaluate whether a simple multimodal embedding baseline produces useful results for fuzzy human product queries.

Do not import experiment code directly into production modules.

When a baseline is selected:
1. record results in this folder,
2. append an evidence entry to `docs/EVIDENCE.md`,
3. promote only the minimal implementation required into `backend/search/`.
